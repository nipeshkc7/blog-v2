{% from "partials/icon.njk" import icon %}

<header class="site-head"{% if site.cover_image %} style="background-image: url('{{ site.cover_image }}')"{% endif %}>
    <div class="container">
        <div class="site-mast">
            {% if site.logo %}
                <div class="site-mast-left">
                    <a {% if page.url == "/" %}aria-current="page"{% endif %} href="{{ site.url }}">
                        <img class="site-logo" src="{{ site.logo }}" alt="{{ site.title }}">
                    </a> 
                     <span>
                       &nbsp;<b>Arpan KC</b>
                    </span>
                </div>
            {% endif %}
            <div class="site-mast-right">
                {% if site.twitter %}
                    <a href="https://twitter.com/{{ site.twitter | replace("@", "") }}" class="site-nav-item" target="_blank" rel="noopener noreferrer">
                        {{ icon({ id: 'twitter' }) }}
                    </a>
                {% endif %}
                {% if site.stack_overflow %}
                    <a href="https://stackoverflow.com/{{ site.stack_overflow }}" class="site-nav-item" target="_blank" rel="noopener noreferrer">
                        {{ icon({ id: 'stack_overflow' }) }}
                    </a>
                {% endif %}
                {% if site.github %}
                    <a href="https://github.com/{{ site.github }}" class="site-nav-item" target="_blank" rel="noopener noreferrer">
                        {{ icon({ id: 'github' }) }}
                    </a>
                {% endif %}
                {% if site.dev %}
                    <a href="https://dev.to/{{ site.dev }}" class="site-nav-item" target="_blank" rel="noopener noreferrer">
                        {{ icon({ id: 'dev' }) }}
                    </a>
                {% endif %}
                {% if site.medium %}
                    <a href="https://medium.com/{{ site.medium }}" class="site-nav-item" target="_blank" rel="noopener noreferrer">
                        {{ icon({ id: 'medium' }) }}
                    </a>
                {% endif %}
                <a class="site-nav-item" href="https://feedly.com/i/subscription/feed/{{ site.url }}/feed.xml" target="_blank" rel="noopener noreferrer">
                    {{ icon({ id: 'rss' }) }}
                </a>
            </div>
        </div>
        {% if page.url == '/' %}
            <div class="site-banner">
                <h1 class="site-banner-title">{{ site.title }}</h1>
                <p class="site-banner-desc">{{ site.description }}</p>
            </div>
        {% endif %}

        {% include 'partials/navigation.njk' %}
    </div>
</header>
